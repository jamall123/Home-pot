# بوت مدير القناة الذكي (Home-pot) 🤖📢

هذا البوت يقوم بإدارة قناتك على تلجرام، حيث يمكنك إرسال أي موضوع له، وسيقوم هو بكتابة منشور احترافي عنه باستخدام الذكاء الاصطناعي (Groq - Llama 3) ونشره في القناة فوراً.

## 🛠️ المتطلبات والإعداد

### 1. تجهيز المفاتيح (Keys) 🔑
قبل البدء، تحتاج إلى جمع البيانات التالية:

1.  **توكن البوت (Bot Token)**:
    - اذهب إلى [BotFather](https://t.me/BotFather) في تلجرام.
    - أرسل `/newbot` واتبع التعليمات لإنشاء بوت جديد.
    - انسخ الـ HTTP API Token.

2.  **مفتاح الذكاء الاصطناعي (Groq API Key)**:
    - اذهب إلى [Groq Cloud](https://console.groq.com/keys).
    - أنشئ حساباً إذا لم يكن لديك، ثم انشئ مفتاح API جديد.

3.  **إعداد القناة**:
    - أنشئ قناة تلجرام (أو استخدم واحدة موجودة).
    - **أضف البوت الذي أنشأته كـ مشرف (Admin)** في القناة (يجب أن يكون لديه صلاحية نشر الرسائل).
    - احصل على معرف القناة (مثلاً `@MyChannelName`).

4.  **معرفك الشخصي (Admin ID)**:
    - لكي لا يستخدم أحد البوت غيرك.
    - اذهب إلى بوت [userinfobot](https://t.me/userinfobot) وانسخ الـ `Id`.

---

### 2. التشغيل 🚀

1.  تأكد من وجود برنامج **Node.js** مثبتاً.
2.  داخل مجلد المشروع، قم بتثبيت المكتبات (إذا لم تكن مثبتة):
    ```bash
    npm install
    ```

### 3️⃣ الاستضافة المجانية الدائمة على Vercel (مستحسن)
هذه الطريقة تجعل البوت يعمل بنظام Webhook، وهو ما يضمن استقراراً وسرعة عالية دون توقف.

1. ارفع المشروع إلى GitHub.
2. أنشئ حساباً في [Vercel.com](https://vercel.com) واربطه بـ GitHub.
3. اضغط **Add New Project** واختر هذا المستودع.
4. في **Environment Variables** أضف نفس المتغيرات (`BOT_TOKEN`، `GROQ_API_KEY`، إلخ).
5. اضغط **Deploy**.
6. **هام جداً (تفعيل الويب هوك):**
   بعد نجاح الرفع، انسخ رابط مشروعك من Vercel (سيكون مثل `https://project-name.vercel.app`).
   الآن، افتح متصفحك وأدخل الرابط التالي (بعد تعديل البيانات):
   
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=<YOUR_VERCEL_URL>/api
   ```
   
   استبدل `<YOUR_BOT_TOKEN>` بتوكن البوت الخاص بك.
   استبدل `<YOUR_VERCEL_URL>` برابط موقعك على Vercel.
   
   إذا ظهرت لك رسالة `Webhook was set`، فمبروك! البوت يعمل الآن 24/7.

## 🛠 التثبيت محلياً (للتجربة)
3.  قم بتغيير اسم ملف `.env.example` إلى `.env` وافتحه لتعديل البيانات:
    ```bash
    mv .env.example .env
    ```
    *(أو قم بإنشاء ملف جديد باسم `.env` والصق المحتويات فيه)*

4.  املأ البيانات في ملف `.env`:
    ```env
    BOT_TOKEN=لصق_توكن_البوت_هنا
    GROQ_API_KEY=لصق_مفتاح_Groq_هنا
    CHANNEL_ID=@اسم_قناتك
    
    # يمكنك استخدام معرفك الرقمي أو اسم المستخدم الخاص بك (بدون @)
    ADMIN_ID=
    ADMIN_USERNAME=اسم_المستخدم_الخاص_بك
    ```

5.  شغل البوت:
    ```bash
    node index.js
    ```

### 3. الاستخدام 📱
- اذهب للبوت الخاص بك في تلجرام واضغط `Start`.
- اكتب له أي موضوع، مثلاً:
  > تحدث عن أهمية شرب الماء
- سيفكر البوت قليلاً 🧠، ثم يكتب منشوراً مرتباً ويرسله لقناتك فوراً! ✅
                                                                                              