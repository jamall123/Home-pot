# 🚀 دليل النشر الشامل

هذا الدليل يشرح كيفية نشر البوت على مختلف المنصات المجانية.

---

## 📋 قبل البدء

تأكد من أن لديك:
- ✅ حساب GitHub
- ✅ البوت يعمل محلياً بنجاح
- ✅ جميع المتغيرات البيئية جاهزة

---

## 1️⃣ Render.com (الأسهل - موصى به)

### الخطوات التفصيلية:

#### أ) رفع الكود إلى GitHub
```bash
# إذا لم تكن قد رفعت الكود بعد
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/Home-pot.git
git push -u origin main
```

#### ب) النشر على Render
1. اذهب إلى [render.com](https://render.com)
2. سجل دخول بحساب GitHub
3. اضغط **New +** → **Web Service**
4. اختر المستودع `Home-pot`
5. املأ الإعدادات:
   ```
   Name: telegram-channel-bot
   Region: اختر الأقرب لك
   Branch: main
   Runtime: Node
   Build Command: npm install
   Start Command: npm start
   ```
6. اختر **Free Plan**
7. أضف المتغيرات البيئية:
   - `BOT_TOKEN` = [توكن البوت]
   - `GROQ_API_KEY` = [مفتاح Groq]
   - `CHANNEL_ID` = [معرف القناة]
   - `ADMIN_ID` = [معرفك]
   - `ADMIN_USERNAME` = [اسم المستخدم]
8. اضغط **Create Web Service**

#### ج) منع التوقف (مهم!)
الخطة المجانية تتوقف بعد 15 دقيقة من عدم النشاط.

**الحل: استخدم UptimeRobot**
1. اذهب إلى [uptimerobot.com](https://uptimerobot.com)
2. أنشئ حساباً مجانياً
3. اضغط **Add New Monitor**
4. اختر:
   ```
   Monitor Type: HTTP(s)
   Friendly Name: Telegram Bot
   URL: [رابط البوت من Render]
   Monitoring Interval: 5 minutes
   ```
5. احفظ ✅

الآن البوت سيعمل 24/7! 🎉

---

## 2️⃣ Railway.app

### الخطوات:
1. اذهب إلى [railway.app](https://railway.app)
2. سجل دخول بـ GitHub
3. اضغط **New Project**
4. اختر **Deploy from GitHub repo**
5. اختر `Home-pot`
6. اضغط **Add Variables** وأضف جميع المتغيرات البيئية
7. Railway سيبدأ النشر تلقائياً ✅

**ملاحظة:** لديك 500 ساعة مجاناً شهرياً (~20 يوم)

---

## 3️⃣ Glitch.com

### الخطوات:
1. اذهب إلى [glitch.com](https://glitch.com)
2. اضغط **New Project** → **Import from GitHub**
3. الصق: `https://github.com/YOUR_USERNAME/Home-pot`
4. بعد الاستيراد، اضغط على **Tools** → **Terminal**
5. في Terminal، اكتب:
   ```bash
   cp .env.example .env
   nano .env
   ```
6. املأ المتغيرات واحفظ (Ctrl+O ثم Enter ثم Ctrl+X)
7. البوت سيعمل تلقائياً ✅

**منع التوقف:**
- استخدم UptimeRobot كما في Render
- URL البوت: `https://YOUR-PROJECT-NAME.glitch.me`

---

## 4️⃣ Replit.com

### الخطوات:
1. اذهب إلى [replit.com](https://replit.com)
2. اضغط **Create Repl**
3. اختر **Import from GitHub**
4. الصق رابط المستودع
5. بعد الاستيراد:
   - اضغط على أيقونة 🔒 **Secrets** في الشريط الجانبي
   - أضف جميع المتغيرات البيئية
6. اضغط **Run** ✅

**منع التوقف:**
- في Replit، اذهب إلى **Always On** (قد يتطلب Replit Core - $7/شهر)
- أو استخدم UptimeRobot مع URL الخاص بـ Replit

---

## 5️⃣ Oracle Cloud (VPS مجاني - للمتقدمين)

### المميزات:
- ✅ مجاني للأبد
- ✅ أداء ممتاز
- ✅ تحكم كامل

### الخطوات:

#### أ) إنشاء VPS
1. اذهب إلى [oracle.com/cloud/free](https://www.oracle.com/cloud/free/)
2. أنشئ حساباً (يحتاج بطاقة ائتمان للتحقق فقط - لن يتم الخصم)
3. بعد التسجيل، اذهب إلى **Compute** → **Instances**
4. اضغط **Create Instance**
5. اختر:
   ```
   Name: telegram-bot
   Image: Ubuntu 22.04
   Shape: VM.Standard.E2.1.Micro (Always Free)
   ```
6. احفظ SSH Key
7. اضغط **Create**

#### ب) الاتصال بالـ VPS
```bash
# من جهازك المحلي
ssh -i /path/to/ssh-key ubuntu@YOUR_VPS_IP
```

#### ج) تثبيت Node.js
```bash
# تحديث النظام
sudo apt update && sudo apt upgrade -y

# تثبيت Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# التحقق
node --version
npm --version
```

#### د) تثبيت البوت
```bash
# تثبيت Git
sudo apt install git -y

# استنساخ المستودع
git clone https://github.com/YOUR_USERNAME/Home-pot.git
cd Home-pot

# تثبيت المكتبات
npm install

# إنشاء ملف .env
nano .env
```

املأ المتغيرات:
```env
BOT_TOKEN=your_bot_token
GROQ_API_KEY=your_groq_key
CHANNEL_ID=@your_channel
ADMIN_ID=your_id
ADMIN_USERNAME=your_username
```

احفظ: `Ctrl+O` → `Enter` → `Ctrl+X`

#### هـ) تشغيل البوت بشكل دائم (PM2)
```bash
# تثبيت PM2
sudo npm install -g pm2

# تشغيل البوت
pm2 start index.js --name telegram-bot

# جعل PM2 يبدأ تلقائياً عند إعادة التشغيل
pm2 startup
pm2 save

# أوامر مفيدة:
pm2 status          # حالة البوت
pm2 logs            # عرض السجلات
pm2 restart telegram-bot  # إعادة التشغيل
pm2 stop telegram-bot     # إيقاف
```

الآن البوت يعمل 24/7 على VPS مجاني! 🎉

---

## 6️⃣ DigitalOcean App Platform

### الخطوات:
1. اذهب إلى [digitalocean.com](https://www.digitalocean.com)
2. أنشئ حساباً (يعطيك $200 رصيد مجاني لمدة 60 يوم)
3. اذهب إلى **App Platform**
4. اضغط **Create App**
5. اختر GitHub واختر المستودع
6. أضف المتغيرات البيئية
7. اختر **Basic Plan** ($5/شهر - مجاني من الرصيد)
8. اضغط **Launch App**

---

## 🔍 التحقق من عمل البوت

بعد النشر على أي منصة:

1. افتح تلجرام
2. اذهب إلى البوت
3. أرسل `/start`
4. أرسل `/status` - يجب أن ترى أن البوت نشط
5. جرب إرسال موضوع: "تحدث عن البرمجة"

إذا عمل كل شيء، تهانينا! 🎉

---

## ⚠️ استكشاف الأخطاء

### البوت لا يرد:
- تحقق من صحة `BOT_TOKEN`
- تأكد من أن البوت قيد التشغيل (راجع logs)

### خطأ 403 عند النشر:
- تأكد من أن البوت مشرف في القناة
- تحقق من صحة `CHANNEL_ID`

### خطأ في Groq API:
- تحقق من صحة `GROQ_API_KEY`
- تأكد من عدم تجاوز الحد المسموح

### البوت يتوقف بعد فترة:
- استخدم UptimeRobot
- أو استخدم VPS (Oracle Cloud)

---

## 📊 مقارنة المنصات

| المنصة | السعر | السهولة | الاستقرار | التوصية |
|--------|-------|---------|-----------|----------|
| Render | مجاني | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | للمبتدئين |
| Railway | مجاني (محدود) | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | جيد |
| Glitch | مجاني | ⭐⭐⭐⭐ | ⭐⭐⭐ | جيد |
| Replit | مجاني | ⭐⭐⭐⭐ | ⭐⭐⭐ | جيد |
| Oracle Cloud | مجاني للأبد | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | للمتقدمين |
| DigitalOcean | $5/شهر | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | احترافي |

---

## 🎯 التوصية النهائية

- **للمبتدئين:** Render.com + UptimeRobot
- **للمتوسطين:** Railway.app
- **للمتقدمين:** Oracle Cloud VPS
- **للاحترافيين:** DigitalOcean أو VPS مدفوع

---

## 📞 الدعم

إذا واجهت مشاكل:
1. راجع السجلات (logs)
2. استخدم `/status` للتحقق من حالة البوت
3. تأكد من جميع المتغيرات البيئية

---

**ملاحظة:** هذا الدليل محدّث حتى ديسمبر 2025. قد تتغير بعض الخطوات مع تحديثات المنصات.
